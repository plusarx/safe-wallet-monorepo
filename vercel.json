{
    "installCommand": "corepack enable && corepack prepare yarn@4.12.0 --activate && yarn workspaces focus @safe-global/web",
    "buildCommand": "yarn workspace @safe-global/web build",
    "outputDirectory": "apps/web/.next",
    "framework": "nextjs"
}